server:
  port: 80

spring:
  application:
    name: atguigu-crowd-zuul

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka

zuul:
  routes:
    crowd-portal:
      service-id: atguigu-crowd-auth
      path: /**
  ignored-services: "*"
